name: Label_check

on:
  issues:
    types: [opened]

jobs:
  comment:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@v6
        with:
          script: |
            const issues_label = github.rest.issues.labels.*.name
            console.log(issues_label)
